import React, { FC } from 'react';
import { CodeEditorWrapper } from './CodeEditor.styled';

interface CodeEditorProps {}

const CodeEditor: FC<CodeEditorProps> = () => (
 <CodeEditorWrapper>
    CodeEditor Component
 </CodeEditorWrapper>
);

export default CodeEditor;
